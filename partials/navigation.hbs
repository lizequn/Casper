
<div class="nav">
    <h3 class="nav-title">Menu</h3>
    <ul>
        {{#foreach navigation}}
            <li class="nav-{{slug}}{{#if current}} nav-current{{/if}}" role="presentation"><a href="{{url absolute="true"}}">{{label}}</a></li>        {{/foreach}}
    </ul>
  
</div>
<span class="nav-cover"></span>
